import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  imageContainer: {
    flex: 1,
  },
  imageBackground: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  artwork: {
    width: 300,
    height: 300,
    zIndex: 1000,
  },
});
